//Uniform input parameters

//Texture
uniform sampler2D texUnit;
uniform float fWidth;
uniform float fHeight;

void main()
{
	
	gl_FragColor = texture2D(texUnit, vec2(1.0 - gl_TexCoord[0].s, gl_TexCoord[0].t));
}



